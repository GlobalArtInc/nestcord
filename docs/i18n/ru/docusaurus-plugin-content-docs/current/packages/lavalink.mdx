```typescript title="app.service.ts"
import { Injectable, Logger } from '@nestjs/common';
import { Context, OnLavalinkManager, OnNodeManager, LavalinkManagerContextOf, NodeManagerContextOf } from '@globalart/nestcord';

@Injectable()
export class AppService {
    private readonly logger = new Logger(AppService.name);

    @OnNodeManager('connect')
    public onConnect(@Context() [node]: NodeManagerContextOf<'connect'>) {
        this.logger.log(`Узел: ${node.options.id} подключен`);
    }

    @OnLavalinkManager('playerCreate')
    public onPlayerCreate(@Context() [player]: LavalinkManagerContextOf<'playerCreate'>) {
        this.logger.log(`Плеер создан на сервере ${player.guildId}`);
    }
}
```

| Название события          | Описание                                                   |
|---------------------------|------------------------------------------------------------|
| `trackStart`              | Выпускается всякий раз, когда воспроизводится трек.        |
| `trackEnd`                | Выпускается всякий раз, когда трек завершает воспроизведение. |
| `trackStuck`              | Выпускается всякий раз, когда трек зависает во время воспроизведения. |
| `trackError`              | Выпускается всякий раз, когда трек выдает ошибку.          |
| `queueEnd`                | Выпускается, когда трек завершился, но в очереди больше нет треков. (`trackEnd` НЕ выполняется) |
| `playerCreate`            | Выпускается всякий раз, когда создается плеер.             |
| `playerMove`              | Выпускается всякий раз, когда плеер перемещается между голосовыми каналами. |
| `playerDisconnect`        | Выпускается всякий раз, когда плеер отключается от канала. |
| `playerSocketClose`       | Выпускается всякий раз, когда сокет узла закрывается для конкретного плеера. |
| `playerDestroy`           | Выпускается всякий раз, когда плеер уничтожается.          |
| `playerUpdate`            | Выпускается всякий раз, когда плеер получает обновление от события playerUpdate Lavalink. |
| `playerMuteChange`        | Выпускается всякий раз, когда изменяется голосовое состояние плеера, связанное с отключением звука. |
| `playerDeafChange`        | Выпускается всякий раз, когда изменяется голосовое состояние плеера, связанное с глушением. |
| `playerSuppressChange`    | Выпускается всякий раз, когда изменяется голосовое состояние плеера, связанное с подавлением. |
| `playerQueueEmptyStart`   | Выпускается всякий раз, когда запускается обработчик пустой очереди (таймаут). |
| `playerQueueEmptyEnd`     | Выпускается всякий раз, когда обработчик пустой очереди завершается (успешно) и уничтожает плеер. |
| `playerQueueEmptyCancel`  | Выпускается всякий раз, когда обработчик пустой очереди отменяется (например, потому что был добавлен новый трек). |
| `playerVoiceJoin`         | Выпускается всякий раз, когда пользователь присоединяется к голосовому каналу плеера. |
| `playerVoiceLeave`        | Выпускается всякий раз, когда пользователь покидает голосовой канал плеера. |
| `debug`                   | Выпускается для нескольких ошибок и логов в lavalink-client, если `managerOptions.advancedOptions.enableDebugEvents` имеет значение `true`. |

## Установка

```bash npm2yarn
npm i lavalink-client
```

- [Руководство по установке Lavalink](https://lavalink.dev/getting-started/index.html)

I'm ready to translate from English (US) to Russian. Please provide the text you'd like me to translate, and I'll return only the translated text without any explanations or extra content.

Когда процесс установки завершен, мы можем импортировать `NestCordLavalinkModule` вместе с вашим `NestCordModule` в корневой `AppModule`:

Ознакомьтесь с дополнительными параметрами модуля в официальной [**документации lavalink-client**](https://lc4.gitbook.io/lavalink-client/docs/lavalinkmanager/manager-options).

## Слушатели

### События LavalinkManager

#### События плагина SponsorBlock

- Это Событие является частью [LavalinkManager Events](#lavalinkmanager-events).
- Это требует добавления [**SponsorBlock Plugin**](https://github.com/topi314/Sponsorblock-Plugin) на сервер Lavalink.

#### События плагина LavaLyrics

- Это событие является частью [Событий LavalinkManager](#lavalinkmanager-events).
- Это требует добавления [**плагина LavaLyrics**](https://github.com/topi314/LavaLyrics) на сервер Lavalink.

### События NodeManager

## Поставщики

## Воспроизведение треков

```typescript title="app.commands.ts"
import { Injectable, UseInterceptors } from '@nestjs/common';
import { NestCordLavalinkService, PlayerManager } from 'lavalink-client';
import { Context, Options, SlashCommand, SlashCommandContext } from '@globalart/nestcord';
import { QueryDto } from './query.dto';
import { SourceAutocompleteInterceptor } from 'source.autocomplete';

@Injectable()
export class AppCommands {
    public constructor(
        private readonly playerManager: PlayerManager,
        private readonly lavalinkService: NestCordLavalinkService
    ) {}

    @UseInterceptors(SourceAutocompleteInterceptor)
    @SlashCommand({
        name: 'play',
        description: 'воспроизвести трек',
    })
    public async onPlay(
        @Context() [interaction]: SlashCommandContext,
        @Options() { query, source }: QueryDto,
    ) {
        const player =
            this.playerManager.get(interaction.guild.id) ??
            this.playerManager.create({
                ...this.lavalinkService.extractInfoForPlayer(interaction),
                // опциональные конфигурации:
                selfDeaf: true,
                selfMute: false,
                volume: 100,
            });

        await player.connect();

        const res = await player.search(
            {
                query,
                source: source ?? 'soundcloud'
            },
            interaction.user.id,
        );

        await player.queue.add(res.tracks[0]);
        if (!player.playing) await player.play();

        // Настоятельно рекомендуется использовать событие `trackStart` для этого объявления
        return interaction.reply({
            content: `Сейчас воспроизводится ${res.tracks[0].info.title}`,
        });
    }
}
```

| Название события        | Описание                                                   |
|-------------------------|-------------------------------------------------------------|
| `LyricsLine`            | Отправляется при получении строки текста песни.           |
| `LyricsFound`           | Отправляется при нахождении текста песни.                 |
| `LyricsNotFound`        | Отправляется при ненахождении текста песни.               |

| Event Name                | Description                                                |
|---------------------------|------------------------------------------------------------|
| `SegmentsLoaded`          | Излучается при загрузке Сегментов.                        |
| `SegmentSkipped`          | Излучается при пропуске конкретного Сегмента.             |
| `ChapterStarted`          | Излучается при начале воспроизведения конкретной Главы.    |
| `ChaptersLoaded`          | Излучается при загрузке Глав.                              |

- Посмотрите в официальной [**документации lavalink-client**](https://tomato6966.github.io/lavalink-client/extra/node-events/).

Превратите своего бота в профессионального диджея с мощью экосистемы [Lavalink](https://lavalink.dev/). Этот пакет использует [lavalink-client](https://github.com/Tomato6966/lavalink-client) за кулисами, обеспечивая высокопроизводительное и эффективное решение для управления аудиопотоками на Discord. Благодаря использованию Lavalink, ваш бот получает возможность управлять воспроизведением аудио, очередями и управлением в реальном времени с минимальной задержкой, превращаясь в полностью функциональную и профессиональную музыкальную систему.

| Event Name                | Description                                                |
|---------------------------|------------------------------------------------------------|
| `create`                  | Генерируется всякий раз при создании Node.                        |
| `destroy`                 | Генерируется всякий раз при уничтожении Node.                      |
| `connect`                 | Генерируется всякий раз при подключении Node.                      |
| `reconnecting`            | Генерируется всякий раз при переподключении Node.                   |
| `reconnectinprogress`     | Генерируется всякий раз при начале переподключения узла. (если у вас есть задержка переподключения, событие reconnecting будет сгенерировано после `retryDelay`.) Полезно для проверки работает ли внутренняя система переподключения узла или нет. |
| `disconnect`              | Генерируется всякий раз при отключении Node.                      |
| `error`                   | Генерируется всякий раз при ошибке Node.                          |
| `raw`                     |  Генерирует каждое событие Node.                            |

```typescript title="source.autocomplete.ts"
import { Injectable } from '@nestjs/common';
import { AutocompleteInteraction } from 'discord.js';
import { DefaultSources } from 'lavalink-client';
import { AutocompleteInterceptor } from '@globalart/nestcord';

@Injectable()
export class SourceAutocompleteInterceptor extends AutocompleteInterceptor {
    public transformOptions(interaction: AutocompleteInteraction) {
        const focused = interaction.options.getFocused(true);
        let choices: string[];

        if (focused.name === 'source') {
            choices = [DefaultSources.soundcloud] // Обратите внимание, что для некоторых источников требуются дополнительные плагины/конфигурация для правильной работы
        }

        return interaction.respond(
        choices
            .filter((choice) => choice.startsWith(focused.value.toString()))
            .map((choice) => ({ name: choice, value: choice })),
        );
    }
}
```

```typescript title="app.module.ts"
import { NestCordLavalinkModule } from '@globalart/nestcord';
import { Module } from '@nestjs/common';
import { Client } from 'discord.js';
import { AppService } from './app.service';

@Module({
    imports: [
        NestCordModule.forRoot({
            token: process.env.DISCORD_TOKEN,
            intents: [
                IntentsBitField.Flags.Guilds,
                IntentsBitField.Flags.GuildVoiceStates
            ],
        }),
        NestCordLavalinkModule.forRoot({
            // Требуется как минимум 1 узел
            nodes: [
                {
                    authorization: 'youshallnotpass',
                    host: '127.0.0.1',
                    port: 2333,
                }
            ]
        })
    ],
    providers: [AppService]
})
export class AppModule {}
```

| Класс (тип для внедрения) | Свойство менеджера (доступ к) | Описание |
|-----------------------------|--------------------------------------|------------------------|
| `LavalinkManager` | `lavalinkManager` | Менеджер Lavalink |
| `NodeManager` | `lavalinkManager.nodeManager` | Менеджер узлов |
| `PlayerManager` | `lavalinkManager (функции плеера)` | Менеджер плеера |

- Смотрите в официальной документации [**lavalink-client**](https://tomato6966.github.io/lavalink-client/extra/manager-events/).

```typescript title="app.service.ts"
import { Injectable } from '@nestjs/common';
import { LavalinkManager, NodeManager } from 'lavalink-client';

@Injectable()
export class AppService {
    public constructor(
        private readonly lavalinkManager: LavalinkManager,
        private readonly nodeManager: NodeManager,
    ) {}
}
```

```typescript title="query.dto.ts"
import { SearchPlatform } from 'lavalink-client';
import { StringOption } from '@globalart/nestcord';

export class QueryDto {
    @StringOption({
        name: 'query',
        description: '<имя | url> запрашиваемого трека'
        required: true
    })
    public readonly query!: string;

    @StringOption({
        name: 'source',
        description: 'источник трека',
        autocomplete: true,
        required: false,
    })
    public readonly source?: SourcePlatform;
}
```