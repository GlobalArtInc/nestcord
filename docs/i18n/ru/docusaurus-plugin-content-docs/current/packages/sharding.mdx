| Событие         | Аргументы            | Описание                                 |
|-----------------|----------------------|------------------------------------------|
| `shardCreate`   | `shard: Shard`       | Испускается при создании нового шарда.   |

:::info
Этот модуль работает в **процессе менеджера** — процессе, который создает и контролирует рабочих процессы шардов. Он отделен от вашего приложения бота.
:::

I appreciate you reaching out, but I notice you've only provided a heading "## Setup" without any actual text to translate.

Please provide the English text you'd like me to translate to Russian, and I'll be happy to help!

Создайте выделенную точку входа для процесса менеджера:

Затем импортируйте `NestCordShardingModule` в ваш модуль менеджера:

### Опции модуля

`NestCordShardingModuleOptions` расширяет Discord.js [`ShardingManagerOptions`](https://discord.js.org/docs/packages/discord.js/main/ShardingManagerOptions:Interface):

`NestCordShardingSpawnOptions`:

## Слушатели

Не забудьте добавить `ShardEventsService` в массив `providers` вашего `ManagerModule`.

### События ShardingManager (`@OnShardingManager`)

### События шарда (`@OnShard`)

Эти слушатели автоматически регистрируются **на каждом шарде** по мере его создания.

Кроме того, используйте оболочку `NestCordShardingService` для удобных методов:

### API `NestCordShardingService`

## Асинхронная конфигурация

Обёртка NestJS для Discord.js [`ShardingManager`](https://discord.js.org/docs/packages/discord.js/main/ShardingManager:Class), которая позволяет управлять осколками бота из контекста выделенного приложения NestJS.

```typescript title="src/manager.ts"
import { NestFactory } from '@nestjs/core';
import { ManagerModule } from './manager.module';

async function bootstrap() {
    await NestFactory.createApplicationContext(ManagerModule);
}

bootstrap();
```

| Событие        | Аргументы                                  | Описание                                                 |
|----------------|--------------------------------------------|----------------------------------------------------------|
| `spawn`        | `shard, process`                           | Издается, когда процесс шарда запущен.                   |
| `ready`        | `shard`                                    | Издается, когда шард становится готовым.                |
| `reconnecting` | `shard`                                    | Издается, когда шард начинает переподключаться.          |
| `resume`       | `shard`                                    | Издается, когда шард возобновляет соединение.            |
| `disconnect`   | `shard`                                    | Издается, когда шард отключается.                        |
| `error`        | `shard, error: Error`                      | Издается, когда шард встречает ошибку.                   |
| `message`      | `shard, message`                           | Издается, когда шард отправляет сообщение менеджеру.    |
| `death`        | `shard, process`                           | Издается, когда процесс шарда завершается.              |

```typescript title="src/manager.service.ts"
import { Injectable } from '@nestjs/common';
import { ShardingManager } from 'discord.js';

@Injectable()
export class ManagerService {
    public constructor(private readonly shardingManager: ShardingManager) {}
}
```

| Метод                     | Возвращает                         | Описание                                                     |
|---------------------------|------------------------------------|--------------------------------------------------------------|
| `shards`                  | `Collection<number, Shard>`        | Все текущие спорненные осколки.                              |
| `spawn(options?)`         | `Promise<Collection<number, Shard>>` | Спорняет осколки (полезно, когда `autoSpawn` равно `false`).|
| `broadcast(message)`      | `Promise<unknown[]>`               | Отправляет сообщение всем осколкам.                          |
| `broadcastEval(fn, opts?)`| `Promise<T[]>`                     | Оценивает функцию на всех клиентах осколков и возвращает результаты.|
| `fetchClientValues(prop)` | `Promise<unknown>`                 | Извлекает свойство клиента из всех осколков.                |
| `respawnAll(options?)`    | `Promise<Collection<number, Shard>>` | Завершает и перезапускает все осколки.                    |

```typescript title="src/manager.service.ts"
import { Injectable } from '@nestjs/common';
import { NestCordShardingService } from '@globalart/nestcord';

@Injectable()
export class ManagerService {
    public constructor(private readonly shardingService: NestCordShardingService) {}

    async getGuildCount(): Promise<number> {
        const counts = await this.shardingService.fetchClientValues('guilds.cache.size');
        return (counts as number[]).reduce((acc, count) => acc + count, 0);
    }

    async broadcastReload(): Promise<void> {
        await this.shardingService.broadcastEval((client) => client.guilds.cache.size);
    }
}
```

```typescript title="src/manager.module.ts"
import { Module } from '@nestjs/common';
import { join } from 'node:path';
import { NestCordShardingModule } from '@globalart/nestcord';

@Module({
    imports: [
        NestCordShardingModule.forRoot({
            file: join(__dirname, 'main.js'),
            token: process.env.DISCORD_TOKEN,
            totalShards: 'auto',
        }),
    ],
})
export class ManagerModule {}
```

```typescript title="src/shard-events.service.ts"
import { Injectable, Logger } from '@nestjs/common';
import { Shard } from 'discord.js';
import {
    OnShardingManager,
    OnShard,
    NestCordShardingManagerEvents,
    NestCordShardEvents,
} from '@globalart/nestcord';

@Injectable()
export class ShardEventsService {
    private readonly logger = new Logger(ShardEventsService.name);

    @OnShardingManager('shardCreate')
    onShardCreate(shard: Shard) {
        this.logger.log(`Фрагмент #${shard.id} создан`);
    }

    @OnShard('ready')
    onShardReady(shard: Shard) {
        this.logger.log(`Фрагмент #${shard.id} готов`);
    }

    @OnShard('error')
    onShardError(shard: Shard, error: Error) {
        this.logger.error(`Ошибка фрагмента #${shard.id}: ${error.message}`);
    }

    @OnShard('death')
    onShardDeath(shard: Shard) {
        this.logger.warn(`Фрагмент #${shard.id} упал`);
    }
}
```

`main.js` — это скомпилированная точка входа вашего фактического приложения бота (того, которое импортирует `NestCordModule`). По умолчанию модуль автоматически вызывает `ShardingManager.spawn()` при инициализации.

| Свойство        | Тип                                    | По умолчанию | Описание                                                           |
|-----------------|----------------------------------------|--------------|--------------------------------------------------------------------|
| `file`          | `string`                               | —            | Путь к скомпилированному файлу скрипта бота.                      |
| `token`         | `string`                               | —            | Токен Discord бота, переданный менеджеру.                         |
| `totalShards`   | `number \| 'auto'`                     | `'auto'`     | Количество шардов для создания.                                    |
| `autoSpawn`     | `boolean`                              | `true`       | Вызывать ли `spawn()` автоматически при инициализации модуля.     |
| `spawnOptions`  | `NestCordShardingSpawnOptions`         | —            | Опции, передаваемые в `ShardingManager.spawn()`.                  |

| Свойство   | Тип                   | По умолчанию | Описание                                                    |
|------------|----------------------|--------------|-------------------------------------------------------------|
| `amount`   | `number \| 'auto'`   | `'auto'`     | Количество шардов для создания.                             |
| `delay`    | `number`             | `5500`       | Задержка в миллисекундах между созданием каждого шарда.    |
| `timeout`  | `number`             | `30000`      | Время ожидания в миллисекундах для готовности шарда.        |

Используйте `@OnShardingManager` и `@OnShard` для прямого реагирования на события жизненного цикла shard в любом провайдере:

## Поставщики

`ShardingManager` можно внедрять непосредственно в любой провайдер:

```typescript title="src/manager.module.ts"
import { Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { join } from 'node:path';
import { NestCordShardingModule } from '@globalart/nestcord';

@Module({
    imports: [
        ConfigModule.forRoot(),
        NestCordShardingModule.forRootAsync({
            imports: [ConfigModule],
            inject: [ConfigService],
            useFactory: (config: ConfigService) => ({
                file: join(__dirname, 'main.js'),
                token: config.getOrThrow('DISCORD_TOKEN'),
                totalShards: 'auto',
            }),
        }),
    ],
})
export class ManagerModule {}
```