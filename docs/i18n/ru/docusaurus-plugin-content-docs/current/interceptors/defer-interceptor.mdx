---
id: перехватчик отсрочки
title: Отложить команду
sidebar_position: 1
---

Перехватчик добавил `interaction.deferReply()` к началу кода

## Использование

```typescript title="src/app.service.ts"
import {
  Context,
  SlashCommand,
  SlashCommandContext,
  DeferCommandInterceptor,
} from '@globalart/nestcord';

@Injectable()
export class AppService {
  @UseInterceptors(DeferCommandInterceptor)
  @SlashCommand({
    name: 'ping',
    description: 'pong',
  })
  async execute(@Context() [interaction]: SlashCommandContext) {
    setTimeout(() => взаимодействие. ollowUp({ content: 'Pong!' }), 5000);
  }
}

```
