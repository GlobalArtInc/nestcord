Перехватчик добавил `interaction.deferReply()` в начало кода

I'm ready to help translate text from English (US) to Russian. Please provide the text you would like me to translate, and I'll return only the translated text without any explanations.

```typescript title="src/app.service.ts"
import {
  Context,
  SlashCommand,
  SlashCommandContext,
  DeferCommandInterceptor,
} from '@globalart/nestcord';

@Injectable()
export class AppService {
  @UseInterceptors(DeferCommandInterceptor)
  @SlashCommand({
    name: 'ping',
    description: 'pong',
  })
  async execute(@Context() [interaction]: SlashCommandContext) {
    setTimeout(() => interaction.followUp({ content: 'Pong!' }), 5000);
  }
}

```