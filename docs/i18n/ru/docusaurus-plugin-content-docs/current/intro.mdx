---
id: интро
slug: /введение
title: Введение
description: Модуль для создания ботов Discord с помощью NestJS, основанный на Discord.js.
sidebar_position: 1
---

<div align="center">
   <h1>
       <a href="#"><img src="/img/logo.png" alt ="NestCord Logo" /></a>
   </h1>
   🤖 Модуль для создания <b><a href="https://discord.com/">Discord</a> ботов</b> с использованием <a href="https://nestjs.com">NestJS</a>, основан на <a href="https://discord.js.org/">Discord. s</a>
   <br/><br/>
   <a href="https://nestcord.js.org">Документация ✨</a> &amp;emsp; <a href="https://github.com/GlobalArtInc/nestcord">Исходный код 🪡</a> &amp;emsp; <a href="https://github.com/GlobalArtInc/nestcord/tree/master/examples">Примеры 🛠️</a> &amp;emsp; <a href="https://discord.gg/4Tc9hssSAv">Сообщество 💬</a>
</div>

<br/>

<p align="center">
    <a href='https://img.shields.io/npm/v/@globalart/nestcord'><img src="https://img.shields.io/npm/v/@globalart/nestcord" alt="NPM Version" /></a>
    <a href='https://img.shields.io/npm/dm/@globalart/nestcord'><img src="https://img.shields.io/npm/dm/@globalart/nestcord" alt="NPM Downloads" /></a>
    <a href='https://img.shields.io/github/last-commit/@GlobalArtInc/nestcord'><img src="https://img.shields.io/github/last-commit/GlobalArtInc/nestcord" alt="Last commit" /></a>
</p>

## О программе

Этот пакет использует лучший из мира NodeJS под капюшоном. [Discord.js](https://github.com/discordjs/discord.js) — самая мощная библиотека
для создания ботов и [Nest.js](https://github.com/nestjs) — это прогрессивный фреймворк для создания хорошо структурированных приложений.
Этот модуль предоставляет быстрый и простой способ для создания ботов Discord и глубокой интеграции с вашим приложением NestJS.

**Возможности**

- Просто. Гибкость. Легко в использовании.
- Возможность создавать собственные декораторы.
- Взаимодействовать с Discord (командами мусора, контекстным меню, компонентами сообщений, списками).
- Полная поддержка охранников NestJS, перехватчиков, фильтров и труб!

For questions and support please use
the [Issues](https://github.com/GlobalArtInc/nestcord/issues/new?assignees=&labels=question&template=question.yml).

## Установка

**Требуется Node.js 16.6.0 или новее.**

```bash npm2yarn
$ npm установить @globalart/nestcord discord.js
```

## Использование

После завершения установки мы можем импортировать `NestCordModule` в корень `AppModule`:

```typescript title="src/app.module.ts"
import { NestCordModule } from '@globalart/nestcord';
import { Module } from '@nestjs/common';
import { IntentsBitField } from 'discord. s';
импорт { AppUpdate } из './app. pdate';

@Module({
    imports: [
        NestCordModule. orRoot({
            токен: 'DISCORD_BOT_TOKEN',
            узел: [IntentsBitField.Flags.Guilds]
        })
    ],
    провайдеров: [AppUpdate]
})
класс экспорта AppModule {}
```

Затем создайте файл `app.update.ts` и добавьте декораторы `On`/`Once` для обработки событий Discord:

```typescript title="src/app.update.ts"
import { Injectable, Logger } from '@nestjs/common';
import { Context On, On, On, ContextOf } from '@globalart/nestcord';
import { Client } from 'discord. s';

@Injectable()
export class AppUpdate {
    private readonly logger = new Logger(AppUpdate. же);

    публичный конструктор (частный клиент для чтения: Клиент) {}

    @Once('ready')
    public onReady(@Context() [client]: ContextOf<'ready'>) {
        this. оггер. og(`Bot зарегистрирован как ${client.user.username}`);
    }

    @On('warn')
    public onWarn(@Context() [message]: ContextOf<'warn'>) {
        это. ogger.warn(сообщение);
    }
}
```

Каждый раз, когда вам нужно обрабатывать любые данные события, используйте декоратор «Контекст».
