## Пользовательские события

И затем вы можете прослушивать пользовательское событие, используя декоратор `@On`.

Также поддерживает пользовательские события.

### События канала

### Гильдийские события

#### События участников гильдии

I understand. I am ready to translate text from English (US) to Russian. Please provide the text you would like me to translate, and I will return only the translated content in Russian without any explanations or additional commentary.

#### События обновления потока

### События сообщений

### События присутствия

Вы можете получить доступ к переменным контекста, используя декоратор `@Context()` в вашей функции, который заполнит переменную массивом аргументов.

```typescript title="src/app.service.ts"
@On('userAvatarUpdate')
public onUserAvatarUpdate(
    @Context() [user, oldAvatar, newAvatar]: ContextOfCustomUserUpdate<'userAvatarUpdate'>
) {
    console.log(
        `Пользователь ${user.tag} изменил свой аватар с ${oldAvatar} на ${newAvatar}`
    );
}
```

#### Журнал аудита событий гильдии

NestCord поддерживает взаимодействие со всеми [событиями Discord](https://discord.js.org/#/docs/main/stable/class/Client#Events) через декораторы `@On` и `@Once`.

Хотя лучше всего использовать более специфичные декораторы, когда это возможно, это полезно, если вы хотите использовать функции, которые NestCord не поддерживает через пользовательские декораторы, взаимодействовать с исходными запросами или прослушивать все события с помощью такого декоратора, как `interactionCreate`.

| Название события                | Описание                                                                          |
|---------------------------------|-----------------------------------------------------------------------------------|
| `guildChannelPermissionsUpdate` | Вызывается при обновлении переопределения прав доступа канала в гильдии.       |

| Event Name                        | Description                                                   |
|-----------------------------------|---------------------------------------------------------------|
| `threadStateUpdate`               | Генерируется каждый раз при обновлении состояния потока.                 |
| `threadNameUpdate`                | Генерируется каждый раз при обновлении имени потока.                  |
| `threadLockStateUpdate`           | Генерируется каждый раз при обновлении состояния блокировки потока.            |
| `threadRateLimitPerUserUpdate`    | Генерируется каждый раз при обновлении лимита скорости на пользователя потока.   |
| `threadAutoArchiveDurationUpdate` | Генерируется каждый раз при обновлении длительности автоматического архивирования потока. |

Вы могли заметить декоратор `@Context` в последнем фрагменте кода: он используется для внедрения контекста события в метод.
Поскольку существует множество типов событий, его тип должен быть выведен из типа `ContextOf<type: string>`.

```typescript title="src/app.service.ts"
@On('messageCreate')
public onMessageCreate(@Context() [message]: ContextOf<'messageCreate'>) {
    console.log(message.content);
}
```

# События пользователя

| Название события       | Описание                                                           |
|------------------------|-------------------------------------------------------------------|
| `voiceChannelJoin`     | Срабатывает, когда участник присоединяется к голосовому каналу.  |
| `voiceChannelLeave`    | Срабатывает, когда участник покидает голосовой канал.            |
| `voiceChannelSwitch`   | Срабатывает, когда участник переключается на другой голосовой канал. |
| `voiceChannelMutestate`| Срабатывает, когда участник отключает микрофон или звук в голосовом канале. |
| `voiceChannelUnmute`   | Срабатывает, когда участник включает микрофон в голосовом канале. |
| `voiceChannelDeaf`     | Срабатывает, когда участник отключает звук в голосовом канале.   |
| `voiceChannelUndeaf`   | Срабатывает, когда участник включает звук в голосовом канале.    |
| `voiceStreamingStart`  | Срабатывает, когда участник начинает трансляцию.                  |
| `voiceStreamingStop`   | Срабатывает, когда участник прекращает трансляцию.                |

```typescript title="src/app.service.ts"
import {Injectable, Logger} from '@nestjs/common';
import {Once, On, Context, ContextOf} from '@globalart/nestcord';

@Injectable()
export class AppService {
    private readonly logger = new Logger(AppService.name);

    @Once('ready')
    public onReady(@Context() [client]: ContextOf<'ready'>) {
        this.logger.log(`Бот вошел как ${client.user.username}`);
    }

    @On('warn')
    public onWarn(@Context() [message]: ContextOf<'warn'>) {
        this.logger.warn(message);
    }
}
```

NestCord «из коробки» поддерживает все события, предоставляемые discord.js. Вы также можете создавать пользовательские события, используя декораторы `@CustomListenerHandler` и `@CustomListener`.

| Имя события              | Описание                                        |
|-------------------------|----------------------------------------------------|
| `rolePositionUpdate`    | Генерируется при обновлении позиции роли.     |
| `rolePermissionsUpdate` | Генерируется при обновлении разрешений роли. |
| `roleIconAdd`           | Генерируется при добавлении иконки роли.           |
| `roleIconRemove`        | Генерируется при удалении иконки роли.         |
| `roleIconUpdate`        | Генерируется при обновлении иконки роли.         |

| Название события          | Описание                                                |
|---------------------------|--------------------------------------------------------|
| `userAvatarUpdate`        | Генерируется при обновлении аватара пользователя.      |
| `userUsernameUpdate`      | Генерируется при обновлении имени пользователя.        |
| `userDiscriminatorUpdate` | Генерируется при обновлении дискриминатора пользователя. |
| `userFlagsUpdate`         | Генерируется при обновлении флагов пользователя.       |

| Event Name                  | Description                                       |
|-----------------------------|---------------------------------------------------|
| `guildMemberBoost`          | Срабатывает при активации бустера участника.     |
| `guildMemberUnboost`        | Срабатывает при деактивации бустера участника.   |
| ` guildMemberRoleAdd`       | Срабатывает при добавлении роли участнику.       |
| `guildMemberRoleRemove`     | Срабатывает при удалении роли у участника.       |
| `guildMemberNicknameUpdate` | Срабатывает при обновлении ника участника.       |
| `guildMemberEntered`        | Срабатывает при входе участника на сервер.       |
| `guildMemberAvatarAdd`      | Срабатывает при добавлении аватара участника.    |
| `guildMemberAvatarRemove`   | Срабатывает при удалении аватара участника.      |
| `guildMemberAvatarUpdate`   | Срабатывает при обновлении аватара участника.    |

| Имя события | Описание |
|---|---|
| `guildMemberOffline` | Генерируется каждый раз, когда участник переходит в режим "не в сети". |
| `guildMemberOnline` | Генерируется каждый раз, когда участник переходит в режим "в сети". |

```typescript title="src/app.service.ts"
import { Injectable } from '@nestjs/common';
import { CustomListener, CustomListenerHandler, BaseHandler, ContextOf } from '@globalart/nestcord';
import { User, UserFlagsBitField } from 'discord.js';

export type CustomUserUpdateEvents = {
	userAvatarUpdate: [user: User, oldAvatar: string, newAvatar: string];
};

export type ContextOfCustomUserUpdate<K extends keyof E, E = CustomUserUpdateEvents> = E[K];


@Injectable()
@CustomListener('userUpdate')
export class UserUpdateHandler extends BaseHandler<CustomUserUpdateEvents> {
	@CustomListenerHandler()
	public handleUserAvatarUpdate([oldUser, newUser]: ContextOf<'userUpdate'>) {
		if (oldUser.partial) return;

		if (oldUser.displayAvatarURL() !== newUser.displayAvatarURL()) {
			this.emit(
				'userAvatarUpdate',
				newUser,
				oldUser.displayAvatarURL(),
				newUser.displayAvatarURL()
			);
		}
	}
}
```

| Название события            | Описание                                                      |
|-----------------------------|------------------------------------------------------------|
| `guildBoostLevelUp`       | Срабатывает при увеличении уровня буста гильдии.          |
| `guildBoostLevelDown`     | Срабатывает при снижении уровня буста гильдии.            |
| `guildBannerAdd`          | Срабатывает при добавлении баннера гильдии.               |
| `guildAfkChannelAdd`      | Срабатывает при добавлении AFK-канала гильдии.            |
| `guildVanityURLAdd`       | Срабатывает при добавлении пользовательского URL гильдии. |
| `guildVanityURLUpdate`    | Срабатывает при обновлении пользовательского URL гильдии. |
| `guildVanityURLRemove`    | Срабатывает при удалении пользовательского URL гильдии.   |
| `guildFeaturesUpdate`     | Срабатывает при обновлении функций гильдии.               |
| `guildAcronymUpdate`      | Срабатывает при обновлении аббревиатуры гильдии.          |
| `guildOwnerUpdate`        | Срабатывает при смене владельца гильдии.                  |
| `guildPartnerAdd`         | Срабатывает при добавлении партнёрского статуса Discord.   |
| `guildPartnerRemove`      | Срабатывает при удалении партнёрского статуса Discord.     |
| `guildVerificationAdd`    | Срабатывает при добавлении уровня проверки гильдии.       |
| `guildVerificationRemove` | Срабатывает при удалении уровня проверки гильдии.         |

:::caution Предупреждение

Если вы используете глобальные фильтры, защиты или перехватчики, они будут срабатывать один раз за событие!
Это означает, что если вы отвечаете на сообщение в защитах, вы можете столкнуться с проблемами дублированных ответов или недействительных взаимодействий.
Посмотрите метаданные `NestCordExecutionContext.getInfo()`, чтобы узнать больше о текущем контексте.

I'm ready to help with translation from English (US) to Russian. However, I don't see any text to translate in your message - you've only provided ":::".

Please provide the English text you'd like me to translate to Russian, and I'll return only the translated version.

| Название события                 | Описание                                                    |
|-----------------------------------|-------------------------------------------------------------|
| `guildAuditLogEntryAdd`           | Срабатывает при добавлении записи в журнал аудита.         |
| `guildAuditLogEntryUpdate`        | Срабатывает при обновлении записи в журнале аудита.        |
| `guildAuditLogEntryDelete`        | Срабатывает при удалении записи из журнала аудита.         |
| `guildAuditLogEntryWebhookCreate` | Срабатывает при создании webhook в журнале аудита.         |
| `guildAuditLogEntryWebhookUpdate` | Срабатывает при обновлении webhook в журнале аудита.       |
| `guildAuditLogEntryWebhookDelete` | Срабатывает при удалении webhook из журнала аудита.        |

#### События обновления роли

| Название события      | Описание                                    |
|------------------------|---------------------------------------------|
| `messagePinned`        | Генерируется при закреплении сообщения.     |
| `messageContentEdited` | Генерируется при обновлении сообщения.      |

### Голосовые события