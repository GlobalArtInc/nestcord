export enum PaginatorTypeEnum {
  BUTTONS = 'buttons',
  SELECT_MENU = 'select_menu',
}

export enum PaginationAction {
  First = 'first',
  Next = 'next',
  Back = 'back',
  Last = 'last',
  Traverse = 'traverse',
}
